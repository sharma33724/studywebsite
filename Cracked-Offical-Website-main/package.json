{
  "name": "prepify-website",
  "version": "1.0.0",
  "description": "Prepify - SAT Preparation Platform",
  "main": "index.js",
  "scripts": {
    "start": "cd client && npm start",
    "build": "cd client && npm run build",
    "dev": "cd client && npm start",
    "install-client": "cd client && npm install",
    "install-functions": "cd functions && npm install",
    "install-all": "npm run install-client && npm run install-functions",
    "deploy": "bash deploy.sh"
  },
  "keywords": ["sat", "prep", "education", "firebase"],
  "author": "Prepify Team",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
